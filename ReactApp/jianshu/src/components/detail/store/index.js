import reducer from './reducer';
import * as actionCreator from './actionCreator';
import * as actionTypes from './actionTypes';

//作为header store的入口文件，全部从这里输出。
export {reducer, actionCreator, actionTypes}